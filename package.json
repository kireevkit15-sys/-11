{
  "name": "diva-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "diva-admin",
    "web",
    "bot",
    "db"
  ],
  "overrides": {
    "minimatch": "^10.2.5",
    "brace-expansion": "^5.0.6",
    "drizzle-orm": "^0.45.2",
    "drizzle-kit": "^0.30.1"
  },
  "scripts": {
    "typecheck": "npm run typecheck -w diva-admin && npm run typecheck -w web && npm run typecheck -w bot",
    "build": "npm run build -w diva-admin && npm run build -w web && npm run build -w bot",
    "dev:admin": "npm run dev -w diva-admin",
    "dev:web": "npm run dev -w web",
    "dev:bot": "npm run dev -w bot"
  }
}
