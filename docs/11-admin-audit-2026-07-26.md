# Комплексный аудит админки diva-admin — 2026-07-26

> 10 параллельных субагентов прошлись по: auth/session/middleware, API endpoints, React components/forms, DB schema/entities, build/config/types, audit/stats/users, uploads/files, tests/CI, content/SEO/UX, env/secrets/deploy. Документ сводит все находки в один перечень с приоритетами.

**Состояние проекта:** TypeScript strict — чисто (0 ошибок), vitest 43/43 passed за 3.5s, Dockerfile multi-stage + non-root, security headers + CSP на месте, iron-session + argon2id.

**Ключевые риски:** пароль администратора уходит в stdout, рассинхрон схемы БД (init.sql ↔ schema.ts ↔ миграции), нет zod-валидации env на старте, нет тестов/CI на diva-admin, нет error/loading/not-found страниц, лиды с `interaction_scheduled` невидимы в UI, нет EXIF-strip / orphan-file cleanup.

---

## CRITICAL — блокирует прод

| # | Файл:строка | Проблема | Что делать |
|---|---|---|---|
| C1 | `diva-admin/scripts/seed-admin.ts:56`, `reset-admin.ts:48` | Сгенерированный пароль администратора печатается в stdout → попадает в `docker logs`, лог-агрегаторы, CI | Записать в tmpfs-файл (`/run/secrets/admin_initial_password`) или требовать явный `ADMIN_INITIAL_PASSWORD` через `.env` |
| C2 | `db/init.sql:21` vs `diva-admin/src/app/admin/leads/status.ts:6` vs `diva-admin/src/lib/entities.ts` | БД имеет 6 статусов (`interaction_scheduled`), UI-код и `statusMeta` — 5. Бот пишет `interaction_scheduled`, админка показывает как «сырую строку» и не считает в фильтрах | Добавить `'interaction_scheduled'` в `LeadStatus`, `LEAD_STATUS_ORDER`, `statusMeta`, фильтры списка. Закрепить `CHECK` в Drizzle через `.$type<LeadStatus>()` |
| C3 | `db/init.sql:197-203` + `ops/docker-compose.prod.yml` | `cleanup_login_attempts()` определена, но **никогда не вызывается**. Нет `pg_cron`/pgAgent. Таблица растёт бесконечно | Включить `pg_cron` в compose, добавить `SELECT cron.schedule('cleanup-login-attempts','*/30 * * * *',$$SELECT cleanup_login_attempts()$$);` в миграцию |
| C4 | `db/migrations/0001_fix_fsi_deadlines.sql:13` | `DROP TABLE fsi_deadlines CASCADE` без бэкапа в автоматической миграции | Обернуть в транзакцию, делать `CREATE TABLE ...new AS SELECT ...` или вынести DDL в ручную операцию с бэкапом |
| C5 | `diva-admin/src/lib/auth.ts:5` + `change-password/route.ts:38` | `change-password` инкрементирует `sessionEpoch`, но **не обновляет epoch в cookie**. После смены пароля middleware сразу выкидывает | В `change-password/route.ts` после `changePassword` заново вызвать `getSession()` и записать `sessionEpoch` через `setSessionUser` |
| C6 | `diva-admin/src/app/api/leads/route.ts` | `POST /api/leads` (новый лид с сайта) не логируется в `audit_log` — слепое пятно в CRM | Добавить `logAudit({ action: 'create', entity: 'leads', entityId: result.id, payload: { source, ip } })` |
| C7 | `diva-admin/src/lib/rate-limit.ts:42-46, 72-74, 81-85` | Rate-limit fail-open при падении БД. Злоумышленник / DDoS обходит блокировку | При сбое БД возвращать `blocked: true`, добавить in-memory circuit breaker |
| C8 | `diva-admin/src/middleware.ts:23` + `api-helpers.ts:28-43` | Middleware не сверяет `sessionEpoch` против БД. Украденный cookie валиден до 24 часов даже после `changePassword` | Поднять `isSessionValid` в middleware (с кэшем 30 сек) или ввести JWT-блок-лист |
| C9 | `diva-admin/src/middleware.ts:45-60` | CSRF-проверка сравнивает `Origin` vs `Host` без доверия к прокси. `Host` подменяется клиентом при прямом доступе без nginx | Использовать `Sec-Fetch-Site: same-origin` или double-submit cookie токен |
| C10 | `diva-admin/src/app/api/[entity]/reorder/route.ts:45-56` | UPDATE применяется ко всей таблице `${entity.table}` без фильтра. Массив `ids` без верхней границы (DoS через 100 000 id) | Добавить `if (ids.length > 1000) return 400`, валидировать что id существуют в таблице |

---

## HIGH — устранить в ближайший спринт

### Auth / session / middleware
- **H1** `diva-admin/.env.example:2` — placeholder `change-me-to-at-least-32-random-bytes` (39 симв.) проходит проверку длины. Если забыть заменить — подделка сессий: оставить пустую строку или `# generate via: openssl rand -hex 32`.
- **H2** `diva-admin/.env:5` — `ADMIN_INITIAL_PASSWORD` пустой. Заполнить.
- **H3** `diva-admin/src/lib/session.ts:83-95` — `getSessionUserFromRequest` в Edge-runtime при ошибке SESSION_SECRET падает с 500. Лучше — 401 с логированием.
- **H4** `diva-admin/src/lib/rate-limit.ts:52-71` — race condition в `recordLoginFailure` (3 независимых SQL без транзакции). Параллельные попытки могут пройти блок.
- **H5** `diva-admin/src/app/api/auth/login/route.ts:13-19` — `clientIp` берётся из `X-Forwarded-For` без `trust proxy`. Без nginx подделывается клиентом → ложный audit.
- **H6** `diva-admin/src/lib/api-helpers.ts:29` — `if (user.sessionEpoch === undefined) return true` — старая сессия без epoch считается валидной. Нужна миграция `UPDATE admin_users SET session_epoch=1 WHERE session_epoch IS NULL` и удалить ветку.
- **H7** `diva-admin/src/lib/auth.ts:101-115` — `validatePasswordStrength` не покрыта тестами. Добавить юнит-тесты + проверку на утечку в timing.
- **H8** `.env:1` — устаревший `STRAPI_TOKEN=2a918e19...` реального вида в корневом `.env`. Strapi удалён, токен — мёртвый секрет. Удалить, отозвать токен, почистить `docs/07-development-setup.md:56,57,100,126-127,267`.

### API endpoints
- **H9** `diva-admin/src/app/api/audit/route.ts:54` + `audit-list.tsx:111` — пагинация фейковая: API возвращает только последние `limit`, клиент «листает» страницы поверх одного запроса. Добавить cursor/offset.
- **H10** `diva-admin/src/app/api/stats/route.ts` + `diva-admin/src/lib/stats.ts:107-117` — нет кеширования, дашборд дёргает БД на каждом рендере. Завернуть в `unstable_cache` с TTL 30 сек.
- **H11** `diva-admin/src/app/api/leads/route.ts:17`, `[entity]/route.ts:29-32`, `leads/[id]/route.ts:29-33` — нет `LIMIT`/`OFFSET`. При росте БД — DoS / утечка всех данных.
- **H12** `diva-admin/src/app/api/users/[id]/route.ts:129`, `users-client.tsx:79-101` — hard-delete пользователя. История audit теряется. Добавить `deletedAt` (soft-delete) и фильтр в middleware API.
- **H13** `db/schema.ts:717-724` vs `db/init.sql:494-504` vs `db/migrations/0000_baseline_schema.sql` — `partners.logo_url` vs `photo_url`. Три источника правды. Выбрать один.
- **H14** `db/schema.ts:117-130` — `subscribers.email` и `telegramUsername` UNIQUE, но nullable → разрешены множественные NULL. Добавить `CHECK ((email IS NOT NULL) OR (telegram_username IS NOT NULL))` + partial unique index.
- **H15** `diva-admin/src/app/api/users/[id]/route.ts:53-63` — смена роли другого админа не инкрементирует его `sessionEpoch`. Старая сессия живёт до конца cookie. Аналогично H5/H6.
- **H16** `diva-admin/src/app/api/auth/login/route.ts:47` — `login_failed` audit пишет без `email` в payload. Невозможно найти все неудачные попытки на конкретный email.

### React / forms / UX
- **H17** `diva-admin/src/app/login/page.tsx:72-99` — нет `autoComplete`, `name`, `autoFocus`, нет «Забыли пароль?», нет «Show password», нет Caps Lock warning. Менеджеры паролей не работают.
- **H18** `diva-admin/src/app/admin/change-password/page.tsx:75-88` — нет `autoComplete="current-password"` / `new-password`. Менеджеры не подставляют.
- **H19** `diva-admin/src/components/entity-form.tsx:104-125` — нет `beforeunload` warning. Закрытие вкладки = потеря несохранённых данных.
- **H20** `diva-admin/src/app/admin/error.tsx`, `not-found.tsx`, `loading.tsx` — отсутствуют. Необработанная RSC-ошибка показывает стоковую страницу Next.js.
- **H21** `diva-admin/src/app/admin/page.tsx:94-96` — `getCounts()` делает N независимых `count()` запросов (до 18 таблиц). На больших данных dead slow. Обернуть в `Promise.all` + индексы.
- **H22** `diva-admin/src/components/entity-list.tsx:309-315` — в DnD-режиме показываются ВСЕ записи. Длинный список (200+) → нет виртуализации, DOM раздувается.
- **H23** `diva-admin/src/app/api/upload/route.ts:44` — лимит 5 МБ проверяется **после** `await request.formData()`. Злоумышленник отправляет 100 ГБ → DoS RAM. Проверять `Content-Length` до парсинга.
- **H24** `diva-admin/src/app/api/upload/route.ts` — нет EXIF stripping. GPS-координаты, модель камеры сохраняются. Особенно критично для `/public/team/*.jpg`. Интегрировать `sharp().rotate().withMetadata({})`.
- **H25** `diva-admin/src/app/api/upload/route.ts` — orphan files не удаляются. Замена аватарки оставляет старый файл на диске навсегда. Диск растёт бесконтрольно.
- **H26** `diva-admin/src/components/image-field.tsx:76-82` + `api-helpers.ts:147` — URL-поле принимает произвольный текст. Обход `javascript:/vbscript:/data:text/html`-фильтра через `\tjavascript:`, SVG-injection, `data:image/svg+xml;base64`. Stored XSS. Усилить whitelist: только `/uploads/`, `https://`, относительные без управляющих символов.

### Build / config / secrets
- **H27** `diva-admin/next.config.ts:46` — CSP `script-src 'self' 'unsafe-inline' 'unsafe-eval'`. Убрать `'unsafe-eval'` для prod (нужен только для HMR dev).
- **H28** `diva-admin/scripts/seed-admin.ts:11-20`, `reset-admin.ts:11-16` — `randomValues[i] % chars.length` — modulo bias ослабляет энтропию. Использовать rejection sampling.
- **H29** `ops/docker-compose.prod.yml` — healthcheck использует `wget`, которого нет в `node:22-alpine` (`Dockerfile` runner-стадия). Заменить на `node -e "..."` или `apk add --no-cache wget`.
- **H30** `diva-admin/src/lib/db.ts:26-30` — `postgres(connectionString, { max: 10 })` без `ssl` опций. Defence-in-depth: добавить `ssl: { rejectUnauthorized: true }` для prod.

### DB / tests / CI
- **H31** `.github/workflows/ci.yml` — нет job для `diva-admin/`. PR в админку проходит без тестов, lint, typecheck. Также остался мёртвый job `cms:` (Strapi) — удалить.
- **H32** `diva-admin/src/lib/auth.ts`, `session.ts`, `api-helpers.ts:27-55` — нет юнит-тестов на критичную security-логику. Тесты только на `coerceBody`, `rbac`, smoke `rate-limit`.
- **H33** `diva-admin/src/lib/__tests__/rate-limit.test.ts:5-7` — smoke-тесты, не поведенческие. Никогда не ловят регрессии из-за fail-open.
- **H34** `diva-admin/src/lib/db.ts:26-33` — postgres-js singleton **без `.end()`**. Утечки в тестах / при HMR. Добавить `process.once('SIGTERM', () => _db?.client.end())`.
- **H35** `diva-admin/src/lib/rate-limit.ts` — нет e2e (Playwright). Login → CRUD → image upload не покрыты.
- **H36** Нет zod-валидации env в `diva-admin` (есть только в `bot/src/env.ts`). `SESSION_SECRET` пустой → админка стартует и падает на первом логине.

---

## MEDIUM

### DB / schema
- **M1** Нет `db.transaction()` в админке. Пары «DB-операция + logAudit» — два независимых запроса. При падении БД между ними — audit пропадает.
- **M2** Нет soft-delete (`deletedAt`) ни в одной таблице. Удалённую запись не восстановить.
- **M3** `db/schema.ts:218-227` (audit_logs) — нет retention policy. Таблица растёт бесконечно.
- **M4** `db/schema.ts:90` — `idx_reminders_fire_at` без `WHERE sent=false` (есть только в init.sql). При миграции через Drizzle частичный индекс теряется.
- **M5** `db/schema.ts:198-210` — нет covering index `(key, attempt_at)` для `recordLoginFailure`.
- **M6** `db/schema.ts:214-215` — `audit_logs.payload jsonb` без whitelist. Любой разработчик может положить туда `password`.
- **M7** `db/init.sql` vs `db/schema.ts` — триггеры `set_updated_at` в init.sql, не в schema.ts. Если накатить только Drizzle-миграции — триггеры не появятся.

### Rate-limit / CSRF
- **M8** `diva-admin/src/lib/auth.ts:53-54` — `DUMMY_HASH` без комментария «это публичная строка, никогда не подставляйте настоящий пароль».
- **M9** `diva-admin/src/lib/rate-limit.ts:32` — `rateKey = ${ip}:${email}`. Подделанным IP можно брутфорсить разные email без блокировки IP. Добавить rate-limit по IP отдельно.
- **M10** `diva-admin/src/lib/db.ts` — email в `rateKey` сохраняется в БД в открытом виде. PII leak в дампе `login_attempts`.

### Stats / leads
- **M11** `diva-admin/src/lib/stats.ts:39-44, 89` — TZ-mismatch между JS-local (`setHours`) и PG-UTC (`date_trunc`).
- **M12** `diva-admin/src/lib/stats.ts:113-114` — `conversion` считает spam в знаменателе.
- **M13** `diva-admin/src/app/api/leads/[id]/route.ts:14` — `ALLOWED_STATUSES` это whitelist значений, не переходов. Любой оператор может `converted → new → spam`. Нет state-machine валидации.
- **M14** `diva-admin/src/app/api/leads/[id]/notes/route.ts:50` — `payload.length: text.length` без hash заметки. Невозможно доказать содержимое при инциденте без раскрытия PII.
- **M15** `diva-admin/src/lib/stats.ts:107-117` — нет тип-валидации входных параметров (`days`, `range`).

### React / a11y
- **M16** `diva-admin/src/components/providers.tsx:115-161` — confirm dialog: нет focus trap, Escape не закрывает, нет initial focus на cancel-кнопке, нет `aria-labelledby`.
- **M17** `diva-admin/src/components/admin-shell.tsx:84-144` — mobile drawer не закрывается по Escape, нет focus trap, нет `inert`/`aria-modal` на фоне.
- **M18** `diva-admin/src/components/admin-shell.tsx:243-248` — пользовательский блок не кликабельный. Нет user menu (dropdown с профилем / logout).
- **M19** `diva-admin/src/components/entity-form.tsx:184-186, 197, 263`, `users-client.tsx:320-398`, `lead-detail.tsx:186-205` — `<label>` без `htmlFor`/`id` на input. Скрин-ридер не связывает.
- **M20** `diva-admin/src/components/providers.tsx:80-112` — toast без `role="status"` / `aria-live="polite"`. Screen reader не озвучивает.
- **M21** `diva-admin/src/app/admin/leads/leads-list.tsx:152-196` — нет пагинации. 500+ leads раздувают DOM. Фильтр работает на клиенте.
- **M22** `diva-admin/src/app/admin/leads/leads-list.tsx:169-173` — строки кликабельны, но это `onClick` → не работает `cmd+click` / Enter.
- **M23** `diva-admin/src/app/admin/users/users-client.tsx:293-421` — modal без focus trap + Escape. Backdrop click закрывает без подтверждения.
- **M24** `diva-admin/src/components/command-palette.tsx` — нет `role="listbox/option"`, нет `aria-activedescendant`, нет focus return.
- **M25** `diva-admin/src/components/theme-toggle.tsx:26-33` — `<button>` без `aria-pressed`.
- **M26** `diva-admin/src/app/admin/audit/audit-list.tsx:85-92` — фильтры строятся из уже загруженных (только последние 100). Неполный список `action` / `entity`.
- **M27** `diva-admin/src/app/admin/audit/audit-list.tsx:170-176` — entity показывается как сырое значение (`case-study`), не переводится в человекочитаемое.
- **M28** `diva-admin/src/components/entity-list.tsx:283-290` — нет debounce на поиске, фильтрация на клиенте.
- **M29** `diva-admin/src/components/admin-shell.tsx` — нет breadcrumbs.
- **M30** `diva-admin/src/app/admin/leads/[id]/lead-detail.tsx:107-127` — optimistic status update без синхронизации `interactionAt`/`updatedAt`.
- **M31** `diva-admin/src/app/admin/leads/status.ts` — 5 цветов/меток, рассинхрон с 6 статусами в БД (см. C2).

### Uploads / files
- **M32** `diva-admin/src/app/api/upload/route.ts:53-56` — `existsSync(join(process.cwd(), '..', 'web'))` хрупкая строковая проверка. В проде volume изолирован, файлы не доходят до `web/public/uploads`.
- **M33** `diva-admin/src/app/api/upload/route.ts:20-34` — magic bytes GIF усечены (`GIF8` вместо `GIF87a`/`GIF89a`).
- **M34** `diva-admin/src/app/api/upload/route.ts` — нет защиты от pixel-flood / image-bomb (PNG декомпрессионная бомба). Нет `sharp().metadata()` с лимитом width/height.
- **M35** `diva-admin/src/next.config.ts:11-13` — `images: { unoptimized: true }`. `/uploads/*.jpg` отдаются без ресайза.
- **M36** `diva-admin/src/components/image-field.tsx` — нет drag&drop, нет paste-from-clipboard.
- **M37** `diva-admin/src/app/api/upload/route.ts` — нет rate-limit (5 файлов/мин на пользователя).
- **M38** `diva-admin/next.config.ts` — нет `Cache-Control` для `/uploads/`.
- **M39** `diva-admin/src/middleware.ts:11, 21` — `/uploads/` отдаётся без auth. По дизайну (для публичных лого), но нет защиты от hotlinking.
- **M40** `diva-admin/src/components/providers.tsx:58-62` — toast `setTimeout` без cleanup. Unmount раньше → warning React 18.

### Build / config / secrets
- **M41** `diva-admin/src/lib/auth.ts:86-98` (`changePassword`) — race в `UPDATE admin_users SET session_epoch = ...` без WHERE на текущее значение. Два параллельных reset → лишние инкременты.
- **M42** `diva-admin/.env` — `diva:diva` пароль БД. Если случайно попадёт на прод — БД открыта.
- **M43** `diva-admin/src/lib/env.ts` (отсутствует) — нет zod env validation. SESSION_SECRET может отсутствовать до первого логина.
- **M44** `diva-admin/components.json:3` — `style: "base-nova"` невалидное значение shadcn. CLI не работает.
- **M45** `diva-admin/src/app/globals.css:8` — `@source "../**/*.{ts,tsx,js,jsx}"` слишком широк — подтянет `web/` и `db/` в CSS-бандл.
- **M46** `diva-admin/tsconfig.json` — нет `noUncheckedIndexedAccess`, `noUnusedLocals`, `noUnusedParameters`. Ловит реальные баги.
- **M47** Нет CORS-политики явно. Админка same-origin, но нигде не задокументировано.
- **M48** `bot/src/env.ts:10-11` — `BOT_ALLOWED_USER_IDS` пустая строка проходит валидацию. В проде должен быть enforced.
- **M49** `diva-admin/src/lib/api-helpers.ts:159-165` — `dbErrorResponse` не различает коды: `P0001` (raise exception "Cannot demote the last admin") показывает generic 500.
- **M50** `ops/docker-compose.prod.yml:153-156` — чувствительные секреты через `environment` (видны в `docker inspect`). Лучше — Docker secrets.
- **M51** `ops/.env.example:8`, `db/.env.example:2` — `POSTGRES_PASSWORD=changeme`. Лучше оставить пустым, чтобы поломка старта заставила заполнить.

---

## LOW

- **L1** `diva-admin/src/lib/entities.ts:48-58` — `FieldConfig` без `min/max/minLength/maxLength/pattern`.
- **L2** `diva-admin/src/lib/api-helpers.ts:67` — `MAX_TEXT_LEN = 50000` общий. `slug varchar(255)` обрезается молча.
- **L3** `db/schema.ts:757, 771, 789` — `heroConfigs`/`footerConfigs`/`announcementMessages` — `serial` PK избыточен, `createdAt` без timezone.
- **L4** `db/schema.ts` — поля `email`, `name`, `message` без length-limit. `email text` принимает 1 ГБ.
- **L5** `db/schema.ts` — нет `citext` для email (case-insensitive).
- **L6** `diva-admin/src/lib/api-helpers.ts:163` — `console.error('[Admin API] DB error:', error)` — Drizzle-ошибка может содержать пользовательские данные. Перейти на `pino` с redact-списком.
- **L7** `diva-admin/src/lib/auth.ts:43-48` — `console.error` без структурного логгера (10 вхождений).
- **L8** `diva-admin/src/middleware.ts` — нет `Strict-Transport-Security`, `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy` — есть в next.config, но **middleware не дублирует** для роутов API (которые могут не проходить через next.config headers, например при прямом обращении к внутреннему серверу).
- **L9** `diva-admin/src/middleware.ts.tmp` — забытый временный файл (700 байт, в git статус `??`). Удалить, добавить `*.tmp` в `.gitignore`.
- **L10** `diva-admin/drizzle.config.ts:8` — `process.env.DATABASE_URL!` без проверки. Лучше явный throw с понятным сообщением.
- **L11** `diva-admin/scripts/reset-admin.ts` — нет `--confirm`, нет env-guard против prod-БД. Можно случайно сбросить прод-пароль.
- **L12** Нет prettier/format script.
- **L13** Нет vitest coverage.
- **L14** `diva-admin/Dockerfile` — нет `HEALTHCHECK` (есть только в compose).
- **L15** `diva-admin/next.config.ts` — `reactStrictMode` не зафиксирован явно.
- **L16** `diva-admin/package.json` — `next lint` deprecated в Next 16.
- **L17** `diva-admin/src/components/command-palette.tsx:64-75` — global keydown Esc закрывает палитру, даже если фокус в input другой формы.
- **L18** `diva-admin/src/components/admin-shell.tsx:184-186, 208-210` — `text-white/30` контраст ≈ 3.4:1. WCAG AA требует 4.5:1.
- **L19** `diva-admin/src/app/globals.css:54-68` — анимации без `prefers-reduced-motion`.
- **L20** `diva-admin/src/app/layout.tsx:11-14` — нет `robots: { index: false, follow: false }`. Админка `/admin/leads` индексируется.

---

## Положительные моменты (не трогать)

- ✅ TypeScript strict — 0 ошибок, ни одного `any`/`@ts-ignore`.
- ✅ Vitest 43/43 passed за 3.5s.
- ✅ Multi-stage Dockerfile + non-root + telemetry disabled.
- ✅ Полный набор security headers + разумный CSP.
- ✅ Argon2id + constant-time dummy hash против user-enumeration.
- ✅ Iron-session: httpOnly, secure (prod), sameSite=lax.
- ✅ Mass-assignment защита: `coerceBody()` отфильтровывает system fields.
- ✅ Upload: magic bytes (SVG запрещён), UUID-имя → нет path traversal.
- ✅ Двойная защита от удаления последнего админа (триггер БД + проверка в коде).
- ✅ RBAC иерархия с admin > editor > viewer.
- ✅ `.gitignore` покрывает `.env`, `.next`, `node_modules`, `uploads/`, `*.log`, `coverage`, `postgres-data/`.
- ✅ `.env` файлы никогда не коммитились.
- ✅ Postgres не выставлен наружу в проде (только внутренняя `diva`-сеть).
- ✅ Seed-аккаунт создаётся с `requirePasswordChange: true` (принудительная смена).

---

## Порядок внедрения (по убыванию ROI)

### Неделя 1 — закрыть CRITICAL
1. C1 — убрать печать пароля в stdout (seed/reset)
2. C2 — добавить `interaction_scheduled` в UI и типы
3. C5 — фикс session_epoch в change-password
4. C6 — `logAudit` в `POST /api/leads`
5. C7 — fail-closed rate-limit при падении БД
6. H29 — фикс healthcheck (wget → node -e)

### Неделя 2 — закрыть auth/session HIGH
7. H1 — `.env.example` без placeholder secret
8. H3 — middleware сверка sessionEpoch с кэшем
9. H4 — race fix в recordLoginFailure (транзакция)
10. H5 — trust proxy для X-Forwarded-For
11. H8 — удалить мёртвый STRAPI_TOKEN
12. H36 — zod env validation в `diva-admin/src/lib/env.ts`
13. H31 — добавить job `diva-admin` в `.github/workflows/ci.yml`

### Неделя 3 — закрыть остальные HIGH
14. C3 — `pg_cron` + `cleanup_login_attempts()`
15. C10 — лимит `ids.length` в reorder
16. H9 — реальная пагинация audit
17. H10 — кеш stats
18. H11 — limit/offset в GET `[entity]` и `/api/leads`
19. H17/H18 — login/change-password UX (autoComplete, name, autofocus)
20. H19 — `beforeunload` в entity-form
21. H20 — error.tsx / not-found.tsx / loading.tsx
22. H23 — `Content-Length` check в upload
23. H24 — EXIF strip через sharp
24. H25 — cleanup orphaned files

### Неделя 4 — UX / a11y MEDIUM
25. H13 — синхронизировать schema.ts ↔ init.sql ↔ migrations
26. H14 — partial unique на `subscribers.email`
27. H16 — `login_failed` с email в payload
28. M16/M17/M23 — focus trap в модалках + Escape
29. M19 — `<label htmlFor>` на формах
30. M21 — пагинация leads
31. H22 — виртуализация entity-list в DnD-режиме
32. H27 — убрать `'unsafe-eval'` из CSP prod

### Технический долг (после прод-запуска)
- M1 — обернуть мутации в `db.transaction()`
- M2 — soft-delete для leads/lead_notes
- M3 — retention policy для audit_logs
- H32-H35 — покрыть тестами auth/session/api
- e2e (Playwright) для login → CRUD → upload

---

## Файлы, которые нужно создать

- `diva-admin/src/lib/env.ts` — zod env validation
- `diva-admin/src/app/admin/error.tsx`
- `diva-admin/src/app/admin/not-found.tsx`
- `diva-admin/src/app/admin/loading.tsx`
- `diva-admin/src/app/global-error.tsx`
- `db/migrations/0002_cleanup_login_attempts_cron.sql` — `pg_cron` расписание
- `db/migrations/0003_add_interaction_scheduled.sql` — привести CHECK к 6 статусам в Drizzle
- `.github/workflows/ci.yml` — добавить job `diva-admin:` + удалить мёртвый `cms:`

## Файлы, которые нужно дополнить / изменить

- `diva-admin/scripts/seed-admin.ts:56`, `reset-admin.ts:48` — не печатать пароль в stdout
- `diva-admin/src/app/api/leads/route.ts` — `logAudit`
- `diva-admin/src/app/api/auth/change-password/route.ts:38` — обновить `sessionEpoch` в cookie
- `diva-admin/src/lib/rate-limit.ts:42-46` — fail-closed при сбое БД
- `diva-admin/src/middleware.ts` — сверка `sessionEpoch` + `Sec-Fetch-Site`
- `diva-admin/src/lib/api-helpers.ts:29` — убрать ветку `sessionEpoch === undefined`
- `diva-admin/src/app/api/audit/route.ts` — реальная пагинация (cursor/offset)
- `diva-admin/src/app/api/upload/route.ts` — `Content-Length` ДО парсинга, sharp EXIF strip, orphan cleanup
- `diva-admin/src/components/image-field.tsx` — whitelist URL
- `diva-admin/src/components/entity-form.tsx` — `beforeunload` + client validation
- `diva-admin/src/app/login/page.tsx` — autoComplete, name, autoFocus, forgot, showPassword, CapsLock, `?next=` redirect
- `diva-admin/src/app/admin/change-password/page.tsx` — autoComplete
- `diva-admin/src/app/admin/page.tsx` — `Promise.all` + скелетон
- `diva-admin/src/components/providers.tsx` — focus trap + Escape + `aria-live`
- `diva-admin/src/components/admin-shell.tsx` — breadcrumbs, focus trap drawer
- `diva-admin/src/components/command-palette.tsx` — `role="listbox/option"`, focus return
- `diva-admin/next.config.ts:46` — убрать `'unsafe-eval'` для prod
- `diva-admin/.env.example` — пустая строка для `SESSION_SECRET`, обязательные ключи
- `diva-admin/.env` — заполнить `ADMIN_INITIAL_PASSWORD`, переименовать в `.env.local`
- `diva-admin/.gitignore` (или корневой) — добавить `*.tmp`
- `diva-admin/src/middleware.ts.tmp` — удалить
- `diva-admin/package.json` — добавить `format`, `coverage`
- `diva-admin/next.config.ts` — фиксировать `reactStrictMode: true`
- `diva-admin/tsconfig.json` — `noUncheckedIndexedAccess`, `noUnusedLocals`
- `diva-admin/Dockerfile` runner — `apk add --no-cache wget` или заменить healthcheck
- `diva-admin/drizzle.config.ts:8` — явная проверка `DATABASE_URL`
- `diva-admin/components.json:3` — `style: "default"`
- `diva-admin/src/app/globals.css:8` — сузить `@source`
- `docs/07-development-setup.md:56,57,100,126-127,267` — убрать упоминания Strapi
- `.env:1-2` — удалить мёртвый `STRAPI_TOKEN`/`STRAPI_URL`
- `.github/workflows/ci.yml` — добавить job `diva-admin:`, удалить мёртвый `cms:`
- `ops/docker-compose.prod.yml` — `pg_cron` в образ Postgres, секреты через `secrets:`
- `diva-admin/src/lib/rbac.ts` — покрыть юнит-тестами
- `diva-admin/src/lib/auth.ts:101-115` — покрыть юнит-тестами
- `diva-admin/src/lib/__tests__/rate-limit.test.ts` — реальные поведенческие тесты (testcontainers)

---

## Метрика качества (текущая vs целевая)

| Метрика | Сейчас | Цель |
|---|---|---|
| TypeScript ошибок | 0 | 0 |
| Unit-тестов | 43 (3 модуля) | 200+ (auth, session, api-helpers, rbac, stats, audit, db) |
| Vitest покрытие | не настроено | > 70% для `lib/` |
| E2E тесты | 0 | Playwright на login → CRUD → upload |
| CI: lint | нет в ci.yml | добавить |
| CI: typecheck | нет в ci.yml | добавить |
| CI: test | нет в ci.yml | добавить |
| CSP `'unsafe-eval'` | есть | убрать для prod |
| Env validation | lazy + runtime | eager на старте (`instrumentation.ts`) |
| CRITICAL HIGH | 10 + 36 | все CRITICAL устранены за неделю 1 |
| a11y: focus trap в модалках | нет | везде |
| a11y: aria-pressed на toggle | нет | везде |
| a11y: `<label htmlFor>` | нет | везде |
| DB schema sources | 3 (init.sql + schema.ts + migrations) | 1 (только миграции) |
| Orphan files cleanup | нет | cron / per-mutation |

---

## Источники / агенты

Отчёт собран из 10 параллельных аудитов:

1. `auth/session/middleware` — auth.ts, session.ts, rate-limit.ts, middleware.ts, login/route.ts
2. `API endpoints security` — все `/api/**/route.ts` + api-helpers.ts + status.ts
3. `React components and forms` — entity-form, entity-list, providers, command-palette, admin-shell, image-field, theme-toggle, login/change-password pages
4. `DB schema and entities` — schema.ts, init.sql, миграции, entities.ts, status.ts, db.ts
5. `Build/config/types` — tsc, next.config, Dockerfile, tsconfig, vitest, globals.css
6. `Audit/stats/users logic` — audit.ts, stats.ts, audit/route.ts, leads/users flows
7. `Uploads and files` — upload/route.ts, image-field.tsx, public/, next.config.ts
8. `Tests and CI` — vitest.config.ts, `__tests__/`, `.github/workflows/`, scripts/
9. `Content and SEO surface` — login/admin pages, shell, dashboard, entities, leads, audit, forms
10. `Env/secrets/deploy` — .env, .env.example, Dockerfile, docker-compose, lib/env.ts образцы

Каждый агент дал список проблем с file:line, severity, фикс. Этот документ — свод с дедупликацией и приоритизацией.
