node_modules/
dist/
.env
.env.local
*.log

# migrations/meta/ (snapshot + _journal.json) ДОЛЖЕН быть в git — drizzle-kit migrate
# на чистом сервере читает список миграций именно из _journal.json, локально
# его негде "regenerate" (см. DEPLOY-BLOCKERS.md, блокер 1). Ранее этот каталог
# был здесь проигнорирован — это и есть причина бага.
